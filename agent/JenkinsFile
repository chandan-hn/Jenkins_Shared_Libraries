pipeline {
    agent any
    stages {
        stage('Build') {
            sh 'ls'
        }

        stage('Test') {
            sh '''
                #!/bin/bash
                ls -ltr
            '''
        }
    }
}
