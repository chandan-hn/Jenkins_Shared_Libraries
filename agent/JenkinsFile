pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'ls'
            }
        }

        stage('Test') {
            steps {
                sh '''
                    #!/bin/bash
                    ls -ltr
                    echo "test stage"
                '''
            }
        }
    }
}
