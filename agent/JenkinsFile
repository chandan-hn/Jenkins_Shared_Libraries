pipeline {
    agent any
    stages {
        stage('Build') {
            step {
                sh 'ls'
            }
        }

        stage('Test') {
            step {
                sh '''
                    #!/bin/bash
                    ls -ltr
                    echo "test stage"
                '''
            }
        }
    }
}
